# Environment variables for the frontend application (apps/web)

# Base URL for the backend API
# Used by the service files (e.g., agentService.ts) to make requests
# Default points to the local backend running via Docker Compose
NEXT_PUBLIC_API_BASE_URL="http://localhost:8000/api/v1"

# Add other frontend-specific environment variables here if needed
# Example: NEXT_PUBLIC_FEATURE_FLAG_NEW_BUILDER="true"

